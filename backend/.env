# Database (for Docker Compose — points to postgres service)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/gym_db

# JWT — CHANGE IN PRODUCTION
SECRET_KEY=24c0614d1d529a8db908d11a7991beec
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS
CORS_ORIGINS=["*"]

# PostgreSQL credentials (used by the postgres service)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=gym_db
